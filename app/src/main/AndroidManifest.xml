<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seba.learnenglish">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SET_TIME_ZONE" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.AddActivity" />
        <activity android:name=".Activity.MyListActivity" />
        <activity android:name=".Activity.EditActivity" />
        <activity android:name=".Activity.FindWordActivity" />
        <activity android:name=".Activity.SettingsActivity" />
        <activity android:name=".Activity.LearnBaseFragmentActivity"></activity>
        <activity android:name=".Activity.RepeatingActivity" />

        <receiver android:name=".NotificationReciever" />
    </application>

</manifest>